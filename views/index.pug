extends layout.pug
//- index (this file!) is what express has been told to render in route 1 of the express app  
//- Using block content we tell layout.pug what to include and where.
block content
  body
    .bounds.app--body
      .app--section--header
        .grid-33
          .app--section--heading
            h2 Timeline
        .grid-33
          .app--section--heading
            strong=followerCount
            h2 Following
        .grid-33
          .app--section--heading
            h2 Direct Messages
      //- Include the 3 display columns
      include timeline.pug
      include following.pug
      include messages.pug
      //- Include the footer bar. This doesn't need to go at the bottom, it can go above the other includes and the css will sort its position out. I'm just placing at the bottom for my own thought process.
      include footer.pug
      //-try putting the below in the head instead?
      script.
  var socket = io.connect('http://localhost:3000');
  socket.on('message1', function(message) {
  alert('The server has a message for you: ' + message);
  })
  
  //- the problem is above where message has been changed to message1. I think it's a pug problem where message appears in messages.pug. Change this and alter css to test this theory.
