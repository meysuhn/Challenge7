extends layout

//- declare main block
block main
    section.namecard
        .namecard-login
            if name
                p.namecard-greeting Welcome, #{name}!
                form.namecard-exit(action='/goodbye', method='POST')
                    button(type='submit')
                        img(src='/static/img/close.svg')
            else
                p.namecard-greeting Hello, student!
        block intro
    block card
      //- this brings in the card pug file as next sibling of the namecard section
      
      //- this is where the user will see 'hello student', their name or goodbye
       // an if else block used to alternatie display depending if a name was present or not
       
